apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: demo-sms
  annotations:
    avp.kubernetes.io/path: "credentials/data/app"
type: Opaque
stringData:
  postgres-password: <postgres-password>